[project]
name = "sy09-projet"
version = "0.1.0"
description = "Projet SY09 — analyse des performances scolaires"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
]

[tool.uv]
package = false
